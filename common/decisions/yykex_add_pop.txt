yyk_add_pop = {
	sound = event_default
	icon = default
	enactment_time = 0

	resources = {
		category = decisions
	}

	potential = {
		exists = owner
		owner = {
			is_ai = no
			has_country_flag = yykex
        }
	}
    effect = {
		owner = { 
            random_owned_pop_group = {
                limit = {
                    species = { is_same_value = prev.species }
                }
				root = {
					create_pop_group = {
						size = 100
						species = prev.species
					}
				}
			}
        }
    }

}