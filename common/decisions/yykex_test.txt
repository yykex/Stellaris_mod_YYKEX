yyk_payback_habitat = {   
	sound = event_default
	icon = default
	enactment_time = 0

	resources = {
		category = decisions
	}

	potential = {
		exists = from
		from = {
			is_ai = no
			has_country_flag = yykex
            NOT = { has_country_flag = yyk_payback_habitat }
        }
        is_capital = yes
	}

	effect = {
        owner = {
            set_country_flag = yyk_payback_habitat
        }
        solar_system = {
            spawn_planet = {
                class = "pc_habitat"
                location = event_target:yykex_capital
                orbit_location = yes
                orbit_angle_offset = 135
                orbit_distance_offset = 9.899
                size = 10
                has_ring = no

                init_effect = {
                    set_planet_entity = {
                        entity = "fc_01_evil_slaver_hab_entity"
                    }
                    set_surveyed = {
                        surveyed = yes
                        surveyor = FROM
                    }
                    set_all_comms_surveyed = yes
                    clear_blockers = yes

                    set_planet_flag = megastructure
                    set_planet_flag = habitat
                    set_planet_flag = habitat_level_1
                    set_planet_flag = payback_habitat

                    add_deposit = d_msi_air_freshener
                    add_deposit = d_payback_habitat_research

                    create_colony = {
                        owner = root.owner
                        species = root.owner
                    }
                    create_pop_group = {
                        size = 100
                        species = owner_main_species
                    }
                    set_name = "NAME_Payback_Warship"
                    #save_event_target_as = yykex_habitat

                    event_target:yykex_capital = {
                        set_planet_flag = habitat@PREV
                        set_planet_flag = has_payback_habitat
                    }
                }
            }
        }
    }
}